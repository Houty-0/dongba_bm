<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dongba.sys.dao.SysRoleDao">

    <sql id="queryWhereId">
        <where>
            <if test="name!=null and name!=''">
                name like concat("%",#{name},"%")
            </if>
        </where>
    </sql>

    <select id="getRowCount" resultType="int">
        select count(*) from sys_roles
        <include refid="queryWhereId"/>
    </select>

    <select id="findPageObjects" resultType="com.dongba.sys.entity.SysRole">
        select * from sys_roles
        <include refid="queryWhereId"/>
        order by createdTime desc
        limit #{startIndex},#{pageSize}
    </select>

    <delete id="deleteObject">
       delete from sys_roles where id =#{id}
    </delete>

    <!-- 插入角色自身信息 (思考:如何获取写入到数据库的主键值)
    1)useGeneratedKeys 表示使用insert操作对应的自增主键值
    2)keyProperty 表示将自增主键值赋值给参数对象的id属性
    -->
    <insert id="insertObject" parameterType="com.dongba.sys.entity.SysRole"
            useGeneratedKeys="true" keyProperty="id">
         insert into sys_roles(id,name,note,createdTime,modifiedTime,createdUser,modifiedUser)
         values
         (null,#{name},#{note},now(),now(),#{createdUser},#{modifiedUser})
    </insert>

    <resultMap type="com.dongba.sys.vo.SysRoleMenuVo" id="sysRoleMenuVo">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="note" property="note"/>
        <collection property="menuIds" column="id"
                    select="com.dongba.sys.dao.SysRoleMenuDao.findMenuIdsByRoleId">
        </collection>
    </resultMap>

    <select id="findObjectById" resultMap="sysRoleMenuVo">
        select id,name,note from sys_roles where id=#{id}
   </select>

    <update id="updateObject" parameterType="com.dongba.sys.entity.SysRole">
       update sys_roles set name=#{name},note=#{note},modifiedUser=#{modifiedUser},modifiedTime=now()
       where id=#{id}
    </update>

    <select id="findObjects" resultType="com.dongba.common.vo.CheckBox">
        select id,name from sys_roles
    </select>

</mapper>